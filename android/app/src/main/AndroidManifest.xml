<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="propertybroker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme behind Flutter UI while engine initializes -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ✅ Android App Links for your hosted URLs -->
            <!-- Opens the app for https://propertybroker-test.web.app/p/... and /b/... -->
            <!-- Also supports the firebaseapp.com mirror domain -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Primary domain -->
                <data android:scheme="https" android:host="propertybroker-test.web.app" android:pathPrefix="/p" />
                <data android:scheme="https" android:host="propertybroker-test.web.app" android:pathPrefix="/b" />

                <!-- Mirror domain (optional but handy) -->
                <data android:scheme="https" android:host="propertybroker-test.firebaseapp.com" android:pathPrefix="/p" />
                <data android:scheme="https" android:host="propertybroker-test.firebaseapp.com" android:pathPrefix="/b" />
            </intent-filter>

        </activity>

        <!-- Required by Flutter (do not remove) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Package visibility for PROCESS_TEXT (Flutter engine) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
